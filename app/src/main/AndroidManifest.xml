<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.administrator.s02e05_thread" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >

        </activity>

        <activity android:name=".HandlerActivity"
            android:label="Handler Activity">


        </activity>

        <activity android:name=".HandlerActivity7"
            android:label="Handler Activity 7">


         </activity>

        <activity android:name=".HandlerActivity8"
            android:label="Handler Activity 8">

        </activity>

        <activity android:name=".HandlerActivity8Sub"
            android:label="handle 8 sub">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            </activity>
    </application>

</manifest>
